# BACKLOG — geo_spatialtrans

Idee e problemi emersi ma non affrontati nella sessione in cui sono nati. Ogni voce: id, sessione di origine, descrizione, dove va affrontata. Una voce si chiude solo con un riferimento al commit/sessione che la risolve.

| id | Origine | Voce | Dove affrontarla | Stato |
|---|---|---|---|---|
| BL-001 | S0 | `full_test.R` e `full_test_visHD.R` divergono anche per `dropout_range` (0.45–0.65 vs 0.25–0.45), non solo per scala/n_cells: due "baseline" con parametri biologici diversi. Decidere quale configurazione rappresenta la pipeline attuale, o parametrizzare. | S2.0 (design Step 2, ricalibrazione da R4) | aperta |
| BL-002 | S0 | Le soglie interne di `full_test.R` (sparsità 30–92 %, UMI medio 3000–20000, CV 0.15–1.0, Moran's I > 0.05) sono valori "a memoria" senza fonte. Vanno sostituite da riferimenti in `BIO_REFERENCES.md` (library size e sparsità a 2/8 µm per archetipo). | R4 | aperta |
| BL-003 | S0 | Al primo passaggio la libreria di progetto non era autosufficiente: 37 namespace (Rcpp, rlang, cli, vctrs, tibble, …) erano risolti dalla site-library di sistema. **Risolto in S0** (passaggi 2–3 di `tools/setup_r_library.R`: 186 pacchetti = chiusura ricorsiva; 0 namespace dal sistema in `results/S0_namespace_origin.csv`; `quarto render` funziona sotto renv isolato). Resta da verificare il restore da zero (BL-008). | — | chiusa (S0) |
| BL-004 | S0 | `DESCRIPTION`: `Imports` elenca pacchetti non usati dal codice attivo (tidyverse, spatstat, fields, tictoc, MASS, cluster) e manca di sf/deldir/polylabelr; `Authors@R` è un placeholder. Da riallineare al codice reale. | S1.6 | aperta |
| BL-005 | S0 | Incoerenza di scala nella pipeline esistente: `pixel_size_um = 1` in `create_sampling_grid()` ma `= 10` in `full_test.R` (design §3). Step 1 non la tocca; Step 2 deve ereditare `metadata$pixel_size_um` (C-A). | S2.0 | aperta |
| BL-006 | S0 | Check C8: l'intervallo [5, 25] µm per l'`eq_radius` mediano è provvisorio (revisione di Daniele). Va sostituito con intervalli per archetipo derivati dai dati reali. | R2–R3 → S1.4 | aperta |
| BL-007 | S0 | I parametri dei 3 preset nuovi (A4 `lymphoid`, A5 `brain_cortex`, A6 `columnar_muscle`) e dei 3 esistenti sono ipotesi: densità e `nucleus_to_eq_ratio` senza fonte. Devono entrare in BIO_REFERENCES prima di S1.2. | R1–R2 | aperta |
| BL-008 | S0 | `renv/activate.R` è della versione renv di aprile 2025; il lockfile è stato rigenerato con renv 1.2.4. Verificare che `renv::restore()` da zero (su una macchina pulita) ricostruisca la libreria — non testato in S0. | S1.6 | aperta |
| BL-009 | S0 | `R/testing/full_simulation_data.rds` legacy (30 MB) resta tracciato in git sotto `legacy/`; valutare `git filter-repo`/LFS o accettare il peso storico. | S1.6 | aperta |
| BL-010 | S0 | `full_test.R`: `cfg$n_cells = 10000` ma la griglia produce 40 000 celle e la pipeline ne sotto-campiona 20 000 "per performance" (log run1). Il parametro non controlla il numero di celle generate. | S2.0 (Step 2 ridefinisce la griglia da `pixel_size_um`) | aperta |
| BL-011 | S0 | `full_test.R`/06e: un "FILTRO HARD" azzera le celle con > 50 000 UMI dopo la validazione (run1: 10–21 celle per chunk, 154 su 20 000 in totale, da `logs/S0_full_test_run1.log`): troncamento non documentato della coda della library size. Da confrontare con la distribuzione reale (R4). | R4 → S2.x | aperta |
| BL-012 | S0 | `renv::snapshot()` segnala ancora pacchetti "required but not installed" (es. crosstalk, DT, foreign): dipendenze dedotte dal codice in `R/`/`vignettes/` o `Suggests` che non fanno parte della chiusura dei 35 pacchetti. Irrilevanti per la pipeline attuale; da rivedere con BL-004/BL-008. | S1.6 | aperta |
| BL-013 | S0 (revisore) | `full_test.R` sovrascrive a ogni run il PNG versionato `R/testing/full_tissue_complex.png`: un test che modifica un file tracciato. Spostare gli output grafici dei test in `results/visualizations/` (ignorato) o nominarli per tag. | S1.5 | aperta |
| BL-014 | R1 | La maschera `in_tissue` di Space Ranger include materiale non parenchimale (linfonodo: ritaglio su grasso/capsula «sicuramente non linfociti», F. Pezzuto). I riferimenti R2–R3 per archetipo vanno stimati su regioni annotate (manuali o da `extract_regions()`), non sull'intera maschera. | R2 (annotazione regioni) ↔ S1.1 | aperta |
| BL-015 | R1 | Soglia B-R1.3 (µm/px ≤ 0.5 per la segmentazione nucleare) senza fonte: il paper di Cellpose (10.1038/s41592-020-01018-x) riporta solo il diametro mediano di training (30 px) e il resize a taglia comune. Sostituire con test empirico: segmentazione a risoluzione nativa (0.274 µm/px) vs sottocampionata 2×/4× sullo stesso ritaglio. | R2 | aperta |
| BL-016 | R1 | Il tag TIFF di risoluzione (XResolution/ResolutionUnit) è 96 dpi in tutte e 5 le immagini 10x: placeholder. L'OME-XML `PhysicalSizeX` è invece corretto (entro 0.08 % da `scalefactors_json`, rilievo del revisore R1). `estimate_pixel_size()` (design C-A): fonti ammissibili in ordine = input esplicito, `scalefactors_json.json` + griglia dei bin, OME-XML verificato contro la griglia; mai il tag di risoluzione. Aggiornare il design §C-A. | S1.5 (design v1.2) | aperta |
| BL-017 | R1 | Cuore (A6): bolle di montaggio nell'H&E (confermate dalla patologa come artefatto di montaggio). Vanno rilevate e mascherate prima della segmentazione (cerchi grigi a bordo netto, diametro ~50–200 µm). | R2 | aperta |
| BL-018 | R1 | A3 (stroma lasso) non ha dataset 10x dedicato: coperto solo da regioni stromali del CRC (A2). Valutare se lo stroma desmoplastico tumorale è un surrogato accettabile dello stroma lasso, o cercare un secondo dataset (es. `Visium_HD_11mm_Human_TA` con polmone/mammella/colon). | R2–R3 | aperta |
| BL-019 | R1 | A1 (`Visium_HD_Mouse_Small_Intestine`) esiste solo con Space Ranger 3.0.0: niente `segmented_outputs`/`barcode_mappings`. Il confronto R2 «Cellpose vs segmentazione Space Ranger» sarà possibile per A2, A4, A5, A6 ma non per A1; in alternativa rielaborare A1 con Space Ranger 4 dai FASTQ (23.9 GB) o dai bin 2 µm. | R2 | aperta |
| BL-020 | R1 (revisore) | Orientamento array→pixel non uniforme fra dataset: in A1–A5 `pxl_col` cresce con `array_col` e `pxl_row` decresce con `array_row`; in A6 i segni sono invertiti (rotazione di 180°); rotazione residua fino a ≈ 0.75° (A2). Documentare e gestire nella mappa bin↔immagine prima di R2 (maschere, regioni). | R2 | aperta |
| BL-021 | R1 (revisore) | 0.36 % (A2, ≈ 29 000) e 0.30 % (A4) dei bin `in_tissue` cadono fuori dall'immagine microscopica: l'area di cattura sporge dal campo acquisito. Da escludere esplicitamente nell'allineamento immagine↔bin. | R2 | aperta |
| BL-022 | R1 (revisore) | Formati immagine eterogenei: A2 `tissue_image.btf` ha una seconda pagina TIFF non leggibile da tifffile; A4 è TIFF classico (non BigTIFF) a strisce. Il lettore immagini di R2 deve gestire entrambi e leggere sempre la serie 0. | R2 | aperta |
| BL-023 | R1 (revisore) | La versione di Space Ranger è letta dal `web_summary.html` con un'euristica (stringa 3.x.y/4.x.y più frequente) quando manca un'etichetta: fragile. Leggere la versione da una fonte strutturata (JSON del web_summary o `metrics_summary`) e confrontarla con la pagina 10x. | R2 | aperta |
